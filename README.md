# Практическая работа №3  
## RESTful веб-сервис на Spring Boot

---

## 📌 Цель работы

Разработать RESTful веб-сервис с использованием фреймворка **Spring**, реализующий доступ к данным в базе данных с поддержкой операций **CRUD** (создание, чтение, обновление, удаление).  
Сервис должен поддерживать форматы **JSON** и **XML**, а также использовать **XSL-преобразование** для отображения XML-ответов в браузере.

---

Для данного приложения был выбран Spring REST, так как в предыдущей работе уже использовался Spring, и не пришлось менять подход к разработке. Spring Boot позволяет быстро запустить проект без сложной настройки, а создание REST API требует минимального количества кода и выглядит наглядно. Spring легко интегрируется с Hibernate и базой данных H2, поэтому работа с данными реализуется без лишних трудностей. Дополнительно удобным является наличие Swagger UI, который позволяет тестировать REST-запросы прямо в браузере.

## 🧰 Используемые технологии

- **Java 25**
- **Spring Boot 3**
- **Spring Web (REST)**
- **Spring Data JPA**
- **Hibernate**
- **H2 Database (in-memory)**
- **Maven**
- **Jackson (JSON + XML)**
- **XSLT**
- **Swagger UI (OpenAPI)**

---

## 🏗 Архитектура приложения

Приложение построено по многоуровневой архитектуре:


Дополнительно:
- DTO используются для передачи данных наружу
- XSL применяется для визуализации XML-ответов

---

## 📚 Предметная область

**Библиотека**

### Сущности:
- **Author**
  - id
  - fullName
  - country
  - birthDate
- **Book**
  - id
  - title
  - isbn
  - publishedYear
  - author (связь Many-to-One)

---

## 🗄 База данных

Используется **H2 (in-memory)** база данных.

- Структура создаётся из `schema.sql`
- Начальные данные загружаются из `data.sql`
- Консоль H2 доступна по адресу: http://localhost:8080/h2-console


Параметры подключения:
- JDBC URL: `jdbc:h2:mem:librarydb`
- User: `sa`
- Password: *(пусто)*

---

## 🔌 REST API

### Authors

| Метод | URL | Описание |
|------|-----|----------|
| GET | `/api/authors` | Получить список авторов |
| GET | `/api/authors/{id}` | Получить автора по id |
| POST | `/api/authors` | Создать автора |
| PUT | `/api/authors/{id}` | Обновить автора |
| DELETE | `/api/authors/{id}` | Удалить автора |
| GET | `/api/authors/{id}/books` | Книги автора |

### Books

| Метод | URL | Описание |
|------|-----|----------|
| GET | `/api/books` | Получить список книг |
| GET | `/api/books/{id}` | Получить книгу по id |
| POST | `/api/books?authorId=…` | Создать книгу |
| PUT | `/api/books/{id}?authorId=…` | Обновить книгу |
| DELETE | `/api/books/{id}` | Удалить книгу |

---

🎨 XSL-преобразование XML

Для XML-ответов используется XSLT-преобразование, позволяющее браузеру отображать XML как HTML-страницу.

XSL-файлы расположены в:

src/main/resources/static/xsl/


В начало XML-ответа автоматически добавляется инструкция:

<?xml-stylesheet type="text/xsl" href="/xsl/authors.xsl"?>


В результате при открытии XML в браузере пользователь видит HTML-таблицу со ссылками между сущностями.

🧪 Тестирование REST API
Swagger UI

Для тестирования REST API используется Swagger UI, который предоставляет веб-интерфейс с кнопками Try it out и Execute.

Swagger UI доступен по адресу:

http://localhost:8080/swagger-ui/index.html


С его помощью можно:

выполнять GET / POST / PUT / DELETE запросы

отправлять JSON-тела запросов

тестировать API прямо в браузере

▶ Запуск проекта
1. Клонирование репозитория
git clone https://github.com/username/library-rest.git
cd library-rest

2. Запуск приложения
mvn spring-boot:run

3. Доступ к сервису

REST API: http://localhost:8080/api/authors

Swagger UI: http://localhost:8080/swagger-ui/index.html

H2 Console: http://localhost:8080/h2-console
