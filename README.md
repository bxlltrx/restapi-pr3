# Практическая работа №3  
## RESTful веб-сервис на Spring Boot

---

## 📌 Цель работы

Разработать RESTful веб-сервис с использованием фреймворка **Spring**, реализующий доступ к данным в базе данных с поддержкой операций **CRUD** (создание, чтение, обновление, удаление).  
Сервис должен поддерживать форматы **JSON** и **XML**, а также использовать **XSL-преобразование** для отображения XML-ответов в браузере.

---

## 🧰 Используемые технологии

- **Java 17**
- **Spring Boot 3**
- **Spring Web (REST)**
- **Spring Data JPA**
- **Hibernate**
- **H2 Database (in-memory)**
- **Maven**
- **Jackson (JSON + XML)**
- **XSLT**
- **Swagger UI (OpenAPI)**

---

## 🏗 Архитектура приложения

Приложение построено по многоуровневой архитектуре:


Дополнительно:
- DTO используются для передачи данных наружу
- XSL применяется для визуализации XML-ответов

---

## 📚 Предметная область

**Библиотека**

### Сущности:
- **Author**
  - id
  - fullName
  - country
  - birthDate
- **Book**
  - id
  - title
  - isbn
  - publishedYear
  - author (связь Many-to-One)

---

## 🗄 База данных

Используется **H2 (in-memory)** база данных.

- Структура создаётся из `schema.sql`
- Начальные данные загружаются из `data.sql`
- Консоль H2 доступна по адресу: http://localhost:8080/h2-console


Параметры подключения:
- JDBC URL: `jdbc:h2:mem:librarydb`
- User: `sa`
- Password: *(пусто)*

---

## 🔌 REST API

### Authors

| Метод | URL | Описание |
|------|-----|----------|
| GET | `/api/authors` | Получить список авторов |
| GET | `/api/authors/{id}` | Получить автора по id |
| POST | `/api/authors` | Создать автора |
| PUT | `/api/authors/{id}` | Обновить автора |
| DELETE | `/api/authors/{id}` | Удалить автора |
| GET | `/api/authors/{id}/books` | Книги автора |

### Books

| Метод | URL | Описание |
|------|-----|----------|
| GET | `/api/books` | Получить список книг |
| GET | `/api/books/{id}` | Получить книгу по id |
| POST | `/api/books?authorId=…` | Создать книгу |
| PUT | `/api/books/{id}?authorId=…` | Обновить книгу |
| DELETE | `/api/books/{id}` | Удалить книгу |

---

## 📄 Поддержка JSON и XML

REST-сервис поддерживает **content negotiation**:

- JSON — по умолчанию
- XML — при заголовке:
```http
Accept: application/xml
Пример: curl -H "Accept: application/xml" http://localhost:8080/api/authors

